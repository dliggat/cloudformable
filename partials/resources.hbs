{
  "{{ lambda-function.id }}"         : {{> lambda-function }},

  "{{ lambda-execution-role.id }}"   : {{> lambda-execution-role }},

  "{{ lambda-execution-policy.id }}" : {{> lambda-execution-policy }},

  "{{ lambda-permission.id }}"       : {{> lambda-permission }},

  "{{ s3-bucket.id }}"               : {{> s3-bucket }},

  "{{ s3-bucket-policy.id }}"        : {{> s3-bucket-policy }}
}
